---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: echoproxy
  namespace: istio-system
spec:
  gateways:
    - gateway
  hosts:
    - "*"
  http:
    - match:
        - uri:
            prefix: /echo/
      rewrite:
        uri: /
      route:
        - destination:
            host: echoproxy.default.svc.cluster.local
            port:
              number: 4001
